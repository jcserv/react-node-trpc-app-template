PORT=3000

# Better Auth secret — must be at least 32 characters
BETTER_AUTH_SECRET=change-me-to-a-random-32-char-secret

# Database path (default: sqlite.db in project root)
DATABASE_PATH=sqlite.db

# CORS origin — set to "false" in production when serving UI from same origin
CORS_ORIGIN=http://localhost:5173
